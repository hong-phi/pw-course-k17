#Lesson 2
##Git
-khi muốn set một username / email mới cho một repo thì ta có thể dùng lệnh 
-git config user.name  "name"
-git config user.email  "email"
-Note : nếu muốn set toàn cục thì dùng ""git config --global user.name  "name" , khi set global sẽ ảnh hưởng toàn bộ tới các repo và chỉ cần set một lần là được
##JavaScript
### Biến
- Có 3 dạng biến là
- var : có thể gán giá trị nhưng không khuyến khích xài vì rất khó kiểm soát được và có thể khai báo trùng tên
- let : có thể gán giá trị nhưng không khai báo trùng tên, thường được sử dụng
- const : là hằng số và không bị thay thế giá trị
### Toán Tử So Sánh
- có những toán tử so sánh như == , > ,<, ===, ==!, =!
- Lưu ý "=" được coi như là phép gán giá trị
### Toán Tử 
- Có các loại toán tử như +, - , * , / và chia hết "%"
### Câu điều kiện
- Câu lệnh If
* Cấu trúc : If (condition){lệnh thực hiện}
* Vòng lặp for : for (let i = x, i < x',i++){lệnh thực hiện}
-Note : i++ có thể hiểu là i = i+1, là bước nhảy khi thực hiện i